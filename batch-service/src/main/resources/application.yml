spring:
  application:
    name: batch-service
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:postgresql://localhost:5432/db
    username: user
    password: pass
#  batch:
#    jdbc:
#      initialize-schema: "always"