spring:
  application:
    name: problems-service
  datasource:
    url: jdbc:postgresql://localhost:5432/db
    username: user
    password: pass
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8000
  sql:
    init:
      mode: always
server:
  port: 8001